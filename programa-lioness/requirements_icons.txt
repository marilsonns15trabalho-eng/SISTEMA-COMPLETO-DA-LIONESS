# Dependências para suporte completo a ícones no PRIME
# Instale com: pip install -r requirements_icons.txt

# Para empacotamento com PyInstaller
pyinstaller>=5.0

# Para empacotamento com cx_Freeze (alternativa)
cx_Freeze>=6.0

# Para criação de atalhos no Windows (opcional)
winshell>=0.6
pywin32>=300

# Para manipulação de imagens (se necessário)
Pillow>=9.0

# Dependências já existentes do projeto
# (adicione aqui as dependências que já estão sendo usadas)

